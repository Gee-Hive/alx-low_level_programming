#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument vector (arrays with argument)
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc; /*marke paramter as unused*/

	printf("%s\n", argv[0]);

	return (0);
}
