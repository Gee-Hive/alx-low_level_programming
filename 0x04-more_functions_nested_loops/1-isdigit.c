#include <stdio>
#include "main.h"

/**
 * _isdigit - checks for a digit passed or not
 * @c: is the input or parameter passed in
 * Return: 0
 */

int _isdigit(int c)
{
	if (c >= 0 && <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
