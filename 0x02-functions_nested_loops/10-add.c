#include <stdio.h>
#include "main.h"

/**
 * add - adds two integer and returns the result
 *
 * @num: is the parameter or input passed in
 * Return: int
 */

int add(int num, int num)
{
	int result = num + num;

	return (result);
}
