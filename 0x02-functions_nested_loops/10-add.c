#include <stdio.h>
#include "main.h"

/**
 * add - adds two integer and returns the result
 *
 * @num: is the parameter or input passed in
 * Return: int
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
